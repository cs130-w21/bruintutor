## Manually running the frontend testing framework. 

run `run npm test`

The tests will run and conclude automatically. However, the mock database will continue to run in the background. To turn off the mock database, use `ctrl-c`.
